<%= render('layouts/error', :object => @comment) %>

<%= form_for [@picture, @comment], html: { multipart: true} do |f| %>
  <!-- <%= f.label :username %>
  <br>
  <%= f.text_field :username %>
  <br> -->
  <!--submit user's name that is currently signed in-->
  <!-- hide id below -->
  <br>
  <%= f.label :comment_body %>
  <br>
  <%= f.text_area :comment_body %>
  <br>
  <p><%= f.submit %></p>
<% end %>
